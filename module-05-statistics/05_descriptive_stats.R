# Load data
data(mtcars)

# Basic statistics
summary(mtcars$mpg)
mean(mtcars$mpg)
median(mtcars$mpg)
sd(mtcars$mpg)  # standard deviation
var(mtcars$mpg) # variance
range(mtcars$mpg)
IQR(mtcars$mpg)

# Summary across dataset
summary(mtcars)
